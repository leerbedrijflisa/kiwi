@using Lisa.Kiwi.Web.Resources

@helper NullCheck(object content)
{
	if (content == null)
	{
		@ReportProperties.ResourceManager.GetString("_null")
	}
	else if (content is bool)
	{
		@ReportProperties.ResourceManager.GetString("_" + content)
	}
	else
	{
		@content
	}
}